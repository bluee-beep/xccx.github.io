# 个人简介内容填充计划

> 关于页四大板块框架已就位（`data/profile.ts` + `pages/about.vue`）。
> 本计划规划「内容填充」阶段：用户提供信息 → 逐板块填入 → 每步验证 + CHANGELOG + git commit。

## 阶段 0：信息收集（需要用户提供的清单）

### 板块一：基本信息
| 字段 | 说明 | 当前占位 |
|---|---|---|
| 姓名 | 展示名（Xccx / 真实姓名 / 英文名？） | `Xccx` |
| 学校 | 学校全称 | `待填写` |
| 专业 | 专业全称 | `待填写` |
| 邮箱 | 真实邮箱（替换 hello@xccx.dev） | `hello@xccx.dev` |
| 社交 | GitHub / Twitter / LinkedIn 真实链接（site.ts） | 占位链接 |

### 板块二：教育背景与技能
| 字段 | 说明 |
|---|---|
| 教育时间线 | 年份、学校、专业/学位、方向描述（1~2 句） |
| 技能分类 | 确认现有三分类（设计/工程/产品）是否符合，替换真实技能词 |

### 板块四：特长兴趣
| 字段 | 说明 |
|---|---|
| 条目 | 3~5 条，每条：名称 + 一句话描述 |

### Bio（自我介绍）
2~3 段：你是谁、在做什么、想成为什么。

### 板块三：项目经历（暂缓）
整理好后按「标题 / 年份 / 描述 / 标签」提供，逐条填入并设计卡片。

## 阶段 1：逐板块填充（每板块一次提交）

```
Step 1 基本信息    → 填 basic + site.ts（邮箱/社交）→ 验证 → CHANGELOG → commit
Step 2 教育背景    → 填 education → 验证 → CHANGELOG → commit
Step 3 技能        → 填 skills → 验证 → CHANGELOG → commit
Step 4 特长兴趣    → 填 interests → 验证 → CHANGELOG → commit
Step 5 Bio        → 填 bio → 验证 → CHANGELOG → commit
```

## 阶段 2：项目经历（用户整理后单独执行）

- 用户提供项目清单 → 填入 `profile.projects` → 卡片样式（标题/年份/描述/标签）→ 空状态占位移除

## 阶段 3：视觉确认与上线

- 关于页视觉走查（编号板块、时间线、卡片、空状态）
- `git push` → GitHub Actions 自动部署 → 线上验证

## 协作规则

1. 每小步完成后更新 CHANGELOG + git commit（既有约定）
2. 设计细节（样式微调）由我合理默认，不满意随时改
3. 用户可随时说「填 X」，我按计划执行对应 Step
